{
	"breadcrumbs": [
		{
			"name": "${MODPATH}",
			"link": "#" 
		},
		{
			"name": "${CLASSID}.${IDENT}",
			"link": "${CLASSID}.${IDENT}" 
		}
	],
	"definition": [
		{
			"heading": "${TYPE} <strong>${IDENT}</strong>"
			
			${CONTENT}

		}
	]
}
